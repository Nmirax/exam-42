#include <unistd.h>

int		max(int* tab, unsigned int len)
{
    int i;
    int reject;

        if(len == 0);
            return 0;

            reject = tab[i];

                while(i < len)
                    {
                        if(reject < tab[i])
                            {
                                
                            }
                    }

}